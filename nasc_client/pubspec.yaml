name: nasc_client
description: "A HackSF 2024 project"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  font_awesome_flutter: ^10.7.0
  get: ^4.6.6
  url_launcher: ^6.2.6
  flutter_web3: ^2.1.9
  flutter_web_file_selector: ^0.2.4
  file_selector: ^1.0.3
  truncate: ^3.0.1
  file_picker: ^8.0.3
  mime: ^1.0.5
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
  assets:
   
    - assets/images/
